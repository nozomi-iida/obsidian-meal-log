# Basic 認証の資格情報。
#
# 2 つとも設定されているときだけ認証が有効になる。片方だけでは未設定として扱う。
# 手元では未設定のまま素通りするが、NODE_ENV=production では要求を通さない。
#
# このファイルは雛形なので値を書かない。実際の値は .env.local に置く。
# .env.local は .gitignore で追跡対象から外れている。

BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
