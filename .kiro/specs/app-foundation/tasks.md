# Implementation Plan

- [ ] 1. 開発基盤の整備

- [x] 1.1 検査と整形の仕組みを導入する
  - 検査ツールの設定ファイルを作成し、依存関係のディレクトリとビルド生成物を対象から外す
  - プロジェクト全体を対象に、検査のみを行うコマンドと自動修正を行うコマンドを分けて用意する
  - **バージョンは範囲指定ではなく具体的な番号で固定する**
  - 意図的に規約違反を含むコードを一時的に置き、検査コマンドが違反箇所のファイルパスと行番号を出力し、非ゼロで終了することを確認したうえで元に戻す
  - **完了状態**: 検査コマンドが既存ファイルを走査して結果を報告し、違反があれば非ゼロで終了する
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - _Boundary: 検査・整形設定_

- [x] 1.2 既存ファイルを整形の書式に揃える
  - 自動修正のコマンドを実行し、`create-next-app` が生成したファイルを規約に沿った書式へ変換する
  - **このタスクの変更は書式のみとする。** ロジックや表示内容の変更を混ぜない
  - **完了状態**: 検査コマンドを実行しても違反が 0 件になる
  - _Requirements: 1.5_
  - _Boundary: 既存コンポーネント_
  - _Depends: 1.1_

- [x] 1.3 自動テストの実行環境を導入する
  - テスト設定ファイルを作成し、DOM 環境・グローバル API・セットアップファイル・パスエイリアスの解決を構成する
  - テスト実行と監視モードのコマンドを用意する。テストが 0 件でも異常終了しない設定にする
  - **バージョンは範囲指定ではなく具体的な番号で固定する**
  - 意図的に失敗するテストを一時的に置き、失敗したテスト名・期待値・実際の値が出力され、非ゼロで終了することを確認したうえで削除する
  - **完了状態**: テストが 1 件も存在しない状態でテストコマンドが実行件数 0 として正常終了し、監視モードではファイル変更で再実行される
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  - _Boundary: テスト設定_
  - 1.1 と同時に進められない。どちらもコマンド定義のためにパッケージ定義ファイルを変更するため

- [x] 1.4 コミット前に検査を自動実行する
  - git の hook を管理する仕組みを導入し、**リポジトリに含める形で** pre-commit を用意する
  - hook は**コミットに含めようとしているファイルのみ**を検査する。プロジェクト全体を走査しない
  - 依存の導入時に hook が自動で有効化されるようにする
  - **バージョンは範囲指定ではなく具体的な番号で固定する**
  - 意図的に違反を含むファイルをステージしてコミットを試み、中止されることを確認する。違反を直せばコミットできることも確認する
  - **完了状態**: 違反を含むファイルをステージするとコミットが中止され、修正後はコミットできる
  - _Requirements: 1.6_
  - _Boundary: 検査・整形設定_
  - _Depends: 1.1_
  - 1.3 と同時に進められない。どちらもパッケージ定義ファイルを変更するため

- [ ] 2. 認証判定とマニフェスト

- [x] 2.1 (P) 認証ヘッダの検証を実装する
  - 実行環境から認証情報を読み出す関数を実装する。**2 つの変数が揃っている場合のみ値を返し、片方でも欠ければ「未設定」を表す値を返す。例外は投げない**
  - 認証ヘッダの値を検証する関数を実装する。HTTP の型に依存せず、文字列を受けて真偽値を返す
  - 比較は定数時間で行う。長さの差から情報が漏れないよう、比較前に固定長へ変換する
  - **どちらの関数も例外を投げない。** 想定外の形式・復号できない値はいずれも偽として扱う
  - 単体テストを 6 件書く（正しい認証情報で真、ヘッダ無しで偽、誤ったパスワードで偽、不正な形式でも偽、2 つ揃っていれば認証情報を返す、片方または両方が欠ければ未設定を返す）
  - **完了状態**: テストコマンドで 6 件すべてが成功する
  - _Requirements: 3.1, 3.2, 3.3, 3.6_
  - _Boundary: 認証判定_
  - _Depends: 1.3_

- [x] 2.2 (P) ホーム画面への追加情報を提供する
  - アプリ名・短縮名・起動 URL・表示方法・背景色・テーマ色を返すマニフェストを用意する
  - 表示方法は全画面での起動を指定し、アドレスバーを伴わない状態にする
  - アイコンは既存のファビコンを参照する。**アイコンのファイルを新規に追加しない**
  - 既存レイアウトのメタデータを、雛形の文言から実際のアプリ名と説明に変更する
  - **完了状態**: 生成された HTML にマニフェストへの参照が出力され、開発者ツールでアプリ名とアイコンが読み取れる
  - _Requirements: 4.1, 4.2, 4.3_
  - _Boundary: アプリマニフェスト_

- [ ] 3. 遮断点の結線

- [x] 3.1 全経路を遮断する関門を実装する
  - 現行のフレームワーク規約に従った関門のファイルを、アプリと同じ階層に置く
  - 認証情報は**要求ごとに読み出し**、判定は 2.1 の検証関数に委譲する。関門自身は判定ロジックを持たない
  - **認証情報が無い場合の振る舞いを環境で分ける。手元では素通りさせ、公開する環境では要求を通さず設定の不足を示す応答を返す**
  - 認証が有効なとき、**適用範囲の除外はマニフェストとアイコンの 2 つに限る**。静的アセットは対象に含める。理由は設計書の遮断点 Implementation Notes を参照
  - 検証を通らない要求には、状態コード 401 と認証を促すヘッダを返す。リダイレクトはしない
  - 失敗の理由を応答から区別できないようにする
  - 必要な環境変数の名称と用途を記したテンプレートファイルを用意する。値は含めない
  - **完了状態**: 認証情報を設定していない状態で開発サーバーを起動すると素通りし、設定して起動し直すと認証が要求される
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_
  - _Boundary: 遮断点_
  - _Depends: 2.1_

- [ ] 4. 動作の確認

- [ ] 4.1 認証の切り替わりを確認する
  - 認証情報を設定しない状態で開発サーバーを起動し、認証を求められずページが表示されることを確認する
  - テンプレートを元に設定ファイルを用意して起動し直し、認証が要求されることを確認する。**手元で使う値は任意でよい。公開する環境で使う値とは別物として扱う**
  - 誤った認証情報では通らず、正しい認証情報では通ることを確認する
  - 公開環境相当の設定で、認証情報を与えずに起動すると要求が通らないことを確認する
  - **完了状態**: 上記 4 つの挙動を確認できている
  - _Requirements: 3.1, 3.2, 3.3, 3.6, 3.7_
  - _Depends: 3.1_

- [ ] 4.2 適用範囲が成立するかを確認する
  - 認証を通過したあと、ページのスタイルとスクリプトが読み込まれ、表示が崩れていないことを確認する
  - 認証が有効な状態で、マニフェストとアイコンが認証を求められずに取得できることを確認する（除外が効いている）
  - 開発者ツールでマニフェストが正常に取得でき、インストール可能と判定されることを確認する
  - **いずれかが失敗した場合は、結果を報告して停止する。** 追加の除外と設計書の更新はこのタスクでは行わない。人が判断したうえで別のタスクとして切る
  - **完了状態**: 3 つの確認結果が報告されている
  - _Requirements: 3.4, 4.1, 4.3_
  - _Depends: 3.1, 2.2_

---

## 補足

### 並列実行について

`(P)` を付けたのは 2.1 と 2.2 の 2 つのみ。認証判定とアプリマニフェストは責務の境界が重ならず、
触れるファイルも異なるため同時に進められる。

以下は並列にできない。

- **1.1 と 1.3** — どちらもコマンド定義のためにパッケージ定義ファイルを変更する。同じファイルを奪い合う
- **1.2 と 2.2** — どちらも既存のレイアウトファイルを触る。1.2 の整形を先に済ませることで順序上解決している
- **3.1** — 2.1 の検証関数に依存する。それが無ければ関門は判定できない

### 仕様書の変更はタスクに含めない

4.2 で確認するはずだった「マニフェスト参照への属性付与」は、2.2 の実装中に**付与されない**ことが
判明した。設計書に記されていた退避策（マニフェストとアイコンのみを適用範囲から外す）の適用は
**アクセス制御の適用範囲の変更**にあたり、Revalidation Triggers に該当する。実装の中で自動的に
行わず、報告して人の判断を仰いだうえで設計書に反映してある。

これ以上の除外が必要になった場合も同じ扱いとする。実装の中で勝手に広げない。

## Implementation Notes

- **1.1**: Biome 2.x は Tailwind CSS v4 の `@theme` などを解釈できず、`globals.css` でパースエラーになる。`css.parser.tailwindDirectives` を有効にすると解消する
- **1.1**: 検査の対象から `public`（外部提供のアセット。ロゴ SVG が a11y ルールに抵触する）と `.kiro`（cc-sdd が管理する仕様書とテンプレート。整形すると更新時に競合する）を外した。設計書は「node_modules と .next を除く全体」としており、**記述と差異がある**
- **1.1**: 整形の字下げを既定のタブからスペース 2 に変更した。既存コードに揃えるため。この結果、`create-next-app` が生成したファイルは既に規約に適合しており、**1.2 の変更対象が無くなった**
- **1.2**: 整形コマンドを実行したが、**変更は 1 件も発生しなかった**。1.1 で字下げを既定のタブからスペース 2 に変えた時点で、`create-next-app` の出力が既に規約に適合していたため。設計時に想定した「既存コードとの衝突」は起きなかった
- **1.4**: 検査ツールに `--staged` があるため、ステージ済みファイルを抽出する別のツール（lint-staged 相当）は要らなかった。依存は hook 管理の 1 つだけで済む
- **1.4**: hook 管理ツールは内部ファイル用の `.gitignore` を自身で生成する。追跡されるのは `pre-commit` 1 ファイルのみで、`core.hooksPath` は自動で設定される
- **1.3**: テスト設定ファイルは `.mts` にする。`.ts` だと `package.json` に `"type": "module"` が無いため CommonJS として読まれ、`import.meta.url` が「将来の Vite で動かなくなる」警告を出す
- **1.3**: 監視モードのコマンドには `--watch` を明示する。省略すると TTY でない環境（スクリプトや CI）では監視にならず 1 回実行して終了する。環境で挙動が変わる書き方は避ける
- **3.1**: `.gitignore` の `.env*` は `.env.local.example` も除外してしまう。`!.env.local.example` を足さないとテンプレートを追跡できない。設計書の Modified Files は「`.gitignore` は変更不要」としており、**記述と差異がある**
- **3.1**: `matcher` の除外パターンではピリオドをエスケープする。`manifest.webmanifest` のままだと正規表現のワイルドカードとして働き、`/manifestXwebmanifest` のような無関係なパスまで除外に落ちる。**Next.js 公式のサンプル（`favicon.ico` など）もエスケープしていないため、そのまま真似すると穴になる**
- **3.1**: Next.js 16 は同一ディレクトリでの `next dev` 多重起動を拒否する（ポートを分けても不可）。環境変数を変えて挙動を比べるときは 1 つずつ起動して停止する
- **2.2**: 生成された HTML の `<link rel="manifest">` に **`crossorigin="use-credentials"` は付与されなかった**。出力は `<link rel="manifest" href="/manifest.webmanifest"/>` のみ。設計書が「付与される想定」としていた前提は**成立しない**。4.2 の確認項目 12 がこの時点で失敗したことになる。対処（`layout.tsx` で手動の `<link>` を出力するか、`matcher` から manifest を除外するか）は人の判断を要するため、このタスクでは行わない
- **2.2**: `background_color` / `theme_color` は設計書に値の指定が無いため、`globals.css` の `--background: #ffffff` と `--foreground: #171717` に揃えた。manifest は単一値しか持てないため、ライトモードの配色を基準にしている
- **2.1**: 環境変数を受け取る関数の引数に `NodeJS.ProcessEnv` を使うと、Next.js がこの型を拡張して `NODE_ENV` を必須にしているため、テストから部分的なオブジェクトリテラルを渡せない。`pnpm test` と検査は通るのに `next build` の型検査だけが落ちる。`Record<string, string | undefined>` を受ければ `process.env` も渡せてテストも書ける。**フレームワークが拡張しうるグローバル型は関数の引数に使わない**。この変更に伴い design.md の Service Interface も更新した
- **1.4**: 検証で「違反あり」「違反なし」の 2 ケースしか試さず、**検査対象が 0 件になるケースを見落とした**。検査の対象外にしたディレクトリ（`.kiro/` など）だけを変更したコミットが、違反が無いのに阻まれていた。`--no-errors-on-unmatched` が要る。**除外設定を持つツールを自動実行に組み込むときは、「除外対象だけが変更された」ケースを必ず試すこと**

---

### 本 spec に含めないもの

要件と設計の合意に従い、以下はタスク化していない。

- ホーム画面への追加と全画面起動の実機確認 — 利用者が別途行う
- 意匠を持つアイコンの作成 — 配置前に別タスクで行う
- 公開環境への配置作業
- 関門を直接呼び出す自動テスト — 実験的な API に依存するため行わず、4.1 の確認で担保する
